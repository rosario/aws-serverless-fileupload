Serverless example - S3 file upload

# Setup

Node version v16.14.2

To install:

> npm i

To run tests

> npm t

# Deployment

It's possible to deploy to the lambda to AWS using `serverless`

Assuming `serverless` is installed globally

> npm i -g serverless

To deploy the lambda to AWS run

> serverless deploy
